{
  "name": "data-center-revenue-token",
  "version": "1.0.0",
  "description": "ERC-20 token for tokenised data centre revenue participation",
  "scripts": {
    "compile": "npx hardhat compile"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@openzeppelin/contracts": "^5.0.2",
    "hardhat": "^2.22.0"
  }
}
